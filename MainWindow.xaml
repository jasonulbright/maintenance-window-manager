<Controls:MetroWindow
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    Title="Maintenance Window Manager"
    Width="1320" Height="820"
    MinWidth="1024" MinHeight="640"
    WindowStartupLocation="CenterScreen"
    TitleCharacterCasing="Normal"
    GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
    NonActiveGlowBrush="{DynamicResource MahApps.Brushes.Accent}"
    BorderThickness="1"
    ShowIconOnTitleBar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Steel.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <ControlTemplate x:Key="SidebarButtonTemplate" TargetType="Button">
                <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      Margin="{TemplateBinding Padding}"
                                      RecognizesAccessKey="True"
                                      SnapsToDevicePixels="True"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter TargetName="Bd" Property="Background">
                            <Setter.Value>
                                <SolidColorBrush Color="#3A3A3A"/>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <Style x:Key="ViewButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square}">
                <Setter Property="Template" Value="{StaticResource SidebarButtonTemplate}"/>
                <Setter Property="Height" Value="42"/>
                <Setter Property="Margin" Value="8,3,8,3"/>
                <Setter Property="Padding" Value="12,0,12,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Controls:ControlsHelper.ContentCharacterCasing" Value="Normal"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background">
                    <Setter.Value><SolidColorBrush Color="#1E1E1E"/></Setter.Value>
                </Setter>
                <Setter Property="BorderBrush">
                    <Setter.Value><SolidColorBrush Color="#555555"/></Setter.Value>
                </Setter>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <Style x:Key="OptionsButtonStyle" TargetType="Button" BasedOn="{StaticResource ViewButtonStyle}"/>

            <Style x:Key="SquareInlineButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square}">
                <Setter Property="Height" Value="28"/>
                <Setter Property="MinWidth" Value="100"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
                <Setter Property="Controls:ControlsHelper.ContentCharacterCasing" Value="Normal"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>

            <!-- Row style owns alternating + IsSelected backgrounds. Setting
                 RowBackground/AlternatingRowBackground on the DataGrid binds
                 those as LOCAL values, which beat Style triggers in WPF's
                 dependency-property precedence chain. Mouse-click selection
                 IS happening (keyboard arrow proves it) but the highlight
                 never rendered with the per-DataGrid setters in play. -->
            <Style x:Key="ViewDataGridRow" TargetType="DataGridRow"
                   BasedOn="{StaticResource MahApps.Styles.DataGridRow}">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ThemeBackground}"/>
                <Style.Triggers>
                    <Trigger Property="AlternationIndex" Value="1">
                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Control.Background}"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.IdealForeground}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ViewDataGrid" TargetType="DataGrid">
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="SelectionMode" Value="Single"/>
                <Setter Property="SelectionUnit" Value="FullRow"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="RowHeaderWidth" Value="0"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="ColumnHeaderHeight" Value="30"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ThemeBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ThemeForeground}"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource MahApps.Brushes.Gray8}"/>
                <Setter Property="AlternationCount" Value="2"/>
                <Setter Property="RowStyle" Value="{StaticResource ViewDataGridRow}"/>
            </Style>

            <Style x:Key="GridCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource MahApps.Styles.DataGridCell}">
                <Setter Property="Padding" Value="6,0,6,0"/>
            </Style>

            <Style x:Key="GridHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource MahApps.Styles.DataGridColumnHeader}">
                <Setter Property="Padding" Value="6,0,6,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
            </Style>

            <Style x:Key="DetailTextStyle" TargetType="TextBox">
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="FontFamily" Value="Cascadia Code, Consolas, Courier New"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="14,14,14,12">
                    <TextBlock x:Name="txtAppTitle" Text="Maintenance Window Manager"
                               FontSize="20" FontWeight="Bold" TextWrapping="Wrap"/>
                    <TextBlock x:Name="txtVersion" Text="v1.0.0" FontSize="10"
                               Foreground="{DynamicResource MahApps.Brushes.Gray1}"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom" Margin="14,0,14,14">
                    <Separator Margin="0,8,0,10"/>
                    <DockPanel LastChildFill="False">
                        <TextBlock x:Name="txtThemeLabel" Text="Dark Theme"
                                   DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="12"/>
                        <Controls:ToggleSwitch x:Name="toggleTheme"
                                               DockPanel.Dock="Right"
                                               IsOn="True"
                                               OnContent="" OffContent=""
                                               MinWidth="0"
                                               ToolTip="Toggle dark / light theme"/>
                    </DockPanel>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Button x:Name="btnViewWindows"   Content="Windows"   Style="{StaticResource ViewButtonStyle}"
                                ToolTip="All maintenance windows across every device collection"/>
                        <Button x:Name="btnViewCoverage"  Content="Coverage"  Style="{StaticResource ViewButtonStyle}"
                                ToolTip="Which collections have / lack maintenance windows -- gap remediation"/>
                        <Button x:Name="btnViewTemplates" Content="Templates" Style="{StaticResource ViewButtonStyle}"
                                ToolTip="Saved schedule templates (Patch Tuesday +N, Daily, Weekly, etc.)"/>

                        <Button x:Name="btnOptions" Content="Options"
                                Style="{StaticResource OptionsButtonStyle}"
                                Margin="8,24,8,3"
                                ToolTip="Site code, SMS provider, defaults, about"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Background="{DynamicResource MahApps.Brushes.Gray8}"/>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="160"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource MahApps.Brushes.Gray10}" Padding="16,12,16,10">
                <StackPanel>
                    <TextBlock x:Name="txtModuleTitle" Text="Windows" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtModuleSubtitle"
                               Text="Configure Site / Provider in Options, then click Refresh."
                               FontSize="11"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource MahApps.Brushes.Gray1}"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Padding="16,10,16,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnRefresh"
                            Grid.Column="0"
                            Content="Refresh"
                            Style="{StaticResource SquareInlineButtonStyle}"
                            MinWidth="120"
                            ToolTip="Reload from MECM (or, on the Templates view, reload templates from disk)"/>

                    <TextBox x:Name="txtFilter"
                             Grid.Column="1"
                             FontSize="12"
                             Padding="6,4,6,4"
                             Margin="8,0,8,0"
                             Controls:TextBoxHelper.Watermark="Filter..."/>

                    <ComboBox x:Name="cboStatusFilter"
                              Grid.Column="2"
                              Width="180"
                              Margin="0,0,8,0"
                              FontSize="12"
                              ToolTip="Limit rows by state">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                    </ComboBox>

                    <Button x:Name="btnExportCsv"  Grid.Column="3" Content="Export CSV"  Style="{StaticResource SquareInlineButtonStyle}"
                            ToolTip="Export the active view to CSV"/>
                    <Button x:Name="btnExportHtml" Grid.Column="4" Content="Export HTML" Style="{StaticResource SquareInlineButtonStyle}"
                            ToolTip="Export the active view to a styled HTML report"/>
                </Grid>
            </Border>

            <Grid Grid.Row="2" Margin="8,0,8,0">

                <!-- ============================================================ -->
                <!-- View: Windows                                                -->
                <!-- ============================================================ -->
                <Grid x:Name="viewWindows" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="240"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="gridWindows"
                              Grid.Row="0"
                              Style="{StaticResource ViewDataGrid}"
                              ColumnHeaderStyle="{StaticResource GridHeaderStyle}"
                              CellStyle="{StaticResource GridCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header=""              Width="32"  Binding="{Binding StatusGlyph}"/>
                            <DataGridTextColumn Header="Collection"    Width="2*"  Binding="{Binding CollectionName}"/>
                            <DataGridTextColumn Header="Window"        Width="2*"  Binding="{Binding WindowName}"/>
                            <DataGridTextColumn Header="Type"          Width="130" Binding="{Binding Type}"/>
                            <DataGridTextColumn Header="Recurrence"    Width="160" Binding="{Binding Recurrence}"/>
                            <DataGridTextColumn Header="Schedule"      Width="2*"  Binding="{Binding Schedule}"/>
                            <DataGridTextColumn Header="Duration"      Width="80"  Binding="{Binding Duration}"/>
                            <DataGridTextColumn Header="Next"          Width="140" Binding="{Binding NextOccurrence}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"
                                  Background="{DynamicResource MahApps.Brushes.Gray8}"
                                  ResizeDirection="Rows"/>

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Padding="0,8,0,8">
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="btnNewWindow"     Content="New Window..."     Style="{StaticResource SquareInlineButtonStyle}" MinWidth="130"
                                        ToolTip="Create a new maintenance window on a target collection"/>
                                <Button x:Name="btnEditWindow"    Content="Edit..."           Style="{StaticResource SquareInlineButtonStyle}" MinWidth="100"
                                        ToolTip="Edit the selected maintenance window"/>
                                <Button x:Name="btnToggleEnabled" Content="Toggle Enabled"    Style="{StaticResource SquareInlineButtonStyle}" MinWidth="130"
                                        ToolTip="Flip the IsEnabled flag on the selected window"/>
                                <Button x:Name="btnRemoveWindow"  Content="Remove..."         Style="{StaticResource SquareInlineButtonStyle}" MinWidth="100"
                                        ToolTip="Delete the selected maintenance window"/>
                            </StackPanel>
                        </Border>

                        <TabControl Grid.Row="1" BorderThickness="0"
                                    Background="{DynamicResource MahApps.Brushes.ThemeBackground}">
                            <TabItem Header="Properties">
                                <TextBox x:Name="txtWindowProperties" Style="{StaticResource DetailTextStyle}"
                                         Text="Select a maintenance window to see its properties."/>
                            </TabItem>
                            <TabItem Header="Next 5 occurrences">
                                <TextBox x:Name="txtNextOccurrences" Style="{StaticResource DetailTextStyle}"
                                         Text="Select a maintenance window to project its next occurrences."/>
                            </TabItem>
                        </TabControl>
                    </Grid>
                </Grid>

                <!-- ============================================================ -->
                <!-- View: Coverage                                               -->
                <!-- ============================================================ -->
                <Grid x:Name="viewCoverage" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="0,8,0,8">
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="btnApplyTemplateBulk" Content="Apply Template..." Style="{StaticResource SquareInlineButtonStyle}" MinWidth="160"
                                    ToolTip="Apply a saved schedule template to one or more collections selected on this view"/>
                        </StackPanel>
                    </Border>

                    <DataGrid x:Name="gridCoverage"
                              Grid.Row="1"
                              SelectionMode="Extended" SelectionUnit="FullRow"
                              Style="{StaticResource ViewDataGrid}"
                              ColumnHeaderStyle="{StaticResource GridHeaderStyle}"
                              CellStyle="{StaticResource GridCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header=""               Width="32"  Binding="{Binding CoverageGlyph}"/>
                            <DataGridTextColumn Header="Collection"     Width="3*"  Binding="{Binding Name}"/>
                            <DataGridTextColumn Header="ID"             Width="100" Binding="{Binding CollectionID}"/>
                            <DataGridTextColumn Header="Members"        Width="80"  Binding="{Binding MemberCount}"/>
                            <DataGridTextColumn Header="Windows"        Width="80"  Binding="{Binding WindowCount}"/>
                            <DataGridTextColumn Header="Built-in"       Width="80"  Binding="{Binding IsBuiltIn}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- ============================================================ -->
                <!-- View: Templates                                              -->
                <!-- ============================================================ -->
                <Grid x:Name="viewTemplates" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="11*" MinWidth="380"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="9*" MinWidth="320"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid x:Name="gridTemplates"
                              Grid.Column="0"
                              Style="{StaticResource ViewDataGrid}"
                              ColumnHeaderStyle="{StaticResource GridHeaderStyle}"
                              CellStyle="{StaticResource GridCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"         Width="*"   Binding="{Binding Name}"/>
                            <DataGridTextColumn Header="Recurrence"   Width="140" Binding="{Binding RecurrenceType}"/>
                            <DataGridTextColumn Header="Type"         Width="160" Binding="{Binding WindowType}"/>
                            <DataGridTextColumn Header="Duration"     Width="100" Binding="{Binding DurationDisplay}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                                  Background="{DynamicResource MahApps.Brushes.Gray8}"
                                  ResizeDirection="Columns"/>

                    <DockPanel Grid.Column="2" Margin="8,0,0,0">
                        <Border DockPanel.Dock="Bottom" Padding="0,8,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="btnNewTemplate"     Content="New Template..."   Style="{StaticResource SquareInlineButtonStyle}" MinWidth="140"
                                        ToolTip="Author a new schedule template"/>
                                <Button x:Name="btnEditTemplate"    Content="Edit..."           Style="{StaticResource SquareInlineButtonStyle}" MinWidth="100"
                                        ToolTip="Edit the selected template"/>
                                <Button x:Name="btnDeleteTemplate"  Content="Delete..."         Style="{StaticResource SquareInlineButtonStyle}" MinWidth="100"
                                        ToolTip="Delete the selected template file"/>
                                <Button x:Name="btnApplyTemplateOne" Content="Apply..."         Style="{StaticResource SquareInlineButtonStyle}" MinWidth="100"
                                        ToolTip="Apply the selected template to one or more collections"/>
                            </StackPanel>
                        </Border>

                        <StackPanel DockPanel.Dock="Top">
                            <TextBlock Text="Description" FontSize="11" FontWeight="SemiBold" Margin="0,8,0,4"/>
                            <TextBlock x:Name="txtTemplateDescription" TextWrapping="Wrap" FontSize="12"
                                       Foreground="{DynamicResource MahApps.Brushes.Gray1}"/>

                            <TextBlock Text="Schedule" FontSize="11" FontWeight="SemiBold" Margin="0,12,0,4"/>
                        </StackPanel>

                        <TextBox x:Name="txtTemplateScheduleSummary"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 FontFamily="Cascadia Code, Consolas, Courier New"
                                 FontSize="12"
                                 Padding="8"
                                 Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
                                 Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}"
                                 BorderThickness="1"
                                 BorderBrush="{DynamicResource MahApps.Brushes.Gray8}"/>
                    </DockPanel>
                </Grid>

                <!-- Progress overlay -->
                <Border x:Name="progressOverlay"
                        Visibility="Collapsed"
                        IsHitTestVisible="False"
                        Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
                        Opacity="0.92">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Controls:ProgressRing IsActive="True" Width="56" Height="56"
                                               Foreground="{DynamicResource MahApps.Brushes.Accent}"/>
                        <TextBlock x:Name="txtProgressTitle"
                                   Text="Loading..."
                                   FontSize="18" FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,14,0,0"/>
                        <TextBlock x:Name="txtProgressStep"
                                   Text=""
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0,6,0,0"
                                   Foreground="{DynamicResource MahApps.Brushes.Gray1}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <GridSplitter Grid.Row="3" Height="5" HorizontalAlignment="Stretch"
                          Background="{DynamicResource MahApps.Brushes.Gray8}"
                          ResizeDirection="Rows"/>

            <Border Grid.Row="4">
                <DockPanel>
                    <TextBlock x:Name="lblLogOutput" DockPanel.Dock="Top" Text="LOG OUTPUT"
                               FontSize="10" FontWeight="SemiBold"
                               Margin="8,6,0,4"/>
                    <TextBox x:Name="txtLog"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Cascadia Code, Consolas, Courier New"
                             FontSize="11"
                             BorderThickness="0"
                             Margin="4,0,4,4"/>
                </DockPanel>
            </Border>

            <Border Grid.Row="5" Padding="8,4,8,4"
                    Background="{DynamicResource MahApps.Brushes.Gray10}">
                <TextBlock x:Name="txtStatus" Text="Ready." FontSize="11"/>
            </Border>
        </Grid>
    </Grid>

</Controls:MetroWindow>
